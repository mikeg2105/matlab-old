file = /home/mike/proj/math/wavelet/btest1.txt
grid = 100 x 100
format = ascii
interleaving = record
majority = row
header = lines 1
field = field0
structure = scalar
type = float
dependency = positions
positions = regular, regular, 0, 1, 0, 1

end
